{
  "name": "@avito-speculant/worker-scraper",
  "type": "module",
  "version": "0.0.3",
  "description": "Avito-Speculant worker-scraper application",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "format": "prettier -w \"src/**/*.ts\"",
    "lint": "eslint \"src/**/*.ts\"",
    "clean": "rimraf .cache dist",
    "prebuild": "npm run clean",
    "build": "tsc -b",
    "build:watch": "tsc -b -w",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@avito-speculant/config": "*",
    "@avito-speculant/logger": "*",
    "@avito-speculant/queue": "*",
    "@avito-speculant/redis": "*",
    "cheerio": "^1.0.0-rc.12",
    "got-scraping": "^4.0.4",
    "node-curl-impersonate": "^1.4.2"
  }
}
