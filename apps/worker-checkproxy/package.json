{
  "name": "@avito-speculant/worker-checkproxy",
  "type": "module",
  "version": "0.0.6",
  "description": "Avito-Speculant worker-checkproxy application",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "format": "prettier -w \"src/**/*.ts\"",
    "lint": "eslint \"src/**/*.ts\"",
    "clean": "rimraf .cache dist",
    "prebuild": "npm run clean",
    "build": "tsc -b",
    "build:watch": "tsc -b -w",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@avito-speculant/config": "*",
    "@avito-speculant/logger": "*",
    "@avito-speculant/redis": "*",
    "@avito-speculant/queue": "*",
    "node-libcurl": "^4.0.0"
  }
}
