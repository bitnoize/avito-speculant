{
  "name": "@avito-speculant/queue",
  "type": "module",
  "version": "0.0.10",
  "description": "Avito-speculant queue library",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "format": "prettier -w \"src/**/*.ts\"",
    "lint": "eslint \"src/**/*.ts\"",
    "clean": "rimraf .cache dist",
    "prebuild": "npm run clean",
    "build": "tsc -b",
    "build:watch": "tsc -b -w"
  },
  "dependencies": {
    "@avito-speculant/logger": "*",
    "@avito-speculant/common": "*",
    "bullmq": "^5.1.0"
  }
}
