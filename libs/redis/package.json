{
  "name": "@avito-speculant/redis",
  "type": "module",
  "version": "0.0.2",
  "description": "Avito-Speculant Redis shared library",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "format": "prettier -w \"src/**/*.ts\"",
    "lint": "eslint \"src/**/*.ts\"",
    "clean": "rimraf .cache dist",
    "prebuild": "npm run clean",
    "build": "tsc -b",
    "build:watch": "tsc -b -w"
  },
  "dependencies": {
    "@avito-speculant/logger": "*",
    "ioredis": "^5.3.2"
  }
}
